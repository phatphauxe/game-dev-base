export const SetStage = (stage) => {
  return {
    type: 'SET_STAGE',
    payload: stage
  }
}

export const SetUpdateRegister = (updateRegister) => {
  return {
    type: 'SET_UPDATE_REGISTER',
    payload: updateRegister
  }
}